```vue
<script setup lang="ts">
import { useSlider, type SliderProps } from '@formwerk/core';

import Thumb from './Thumb.vue';

const props = defineProps<SliderProps>();

const {
  trackProps,
  groupProps,
  labelProps,
  outputProps,
  errorMessage,
  fieldValue,
  errorMessageProps,
  useThumbMetadata,
} = useSlider(props);

const thumbData = useThumbMetadata(0);
</script>

<template>
  <div class="slider" v-bind="groupProps">
    <div v-bind="labelProps">{{ label }}</div>
    <div v-bind="trackProps" class="track">
      <Thumb />
    </div>
    <output v-bind="outputProps">Value: {{ fieldValue }}</output>

    <div v-if="errorMessage" v-bind="errorMessageProps">
      {{ errorMessage }}
    </div>
  </div>
</template>

<style scoped>
.slider {
  .track {
    display: flex;
    align-items: center;
    width: 150px;
    margin-top: 12px;
    margin-bottom: 12px;
    height: 6px;
    background-color: #78716c;
    border-radius: 6px;

    &::before {
      content: '';
      width: calc(v-bind('thumbData.percent') * 100%);
      background-color: #10b981;
      border-radius: 6px;
      height: 6px;
    }
  }
}
</style>
```
