```vue
<script setup lang="ts">
import { type CheckboxProps, useCheckbox } from '@formwerk/core';

const props = defineProps<CheckboxProps>();

const { labelProps, inputProps, errorMessage } = useCheckbox(props);
</script>

<template>
  <div>
    <label v-bind="labelProps" :style="{ display: 'block' }">
      <input v-bind="inputProps" />
      {{ label }}
    </label>

    <div v-if="errorMessage" v-bind="errorMessageProps">
      {{ errorMessage }}
    </div>
  </div>
</template>
```
