```vue collapse={23-53}
<script setup lang="ts">
import { useSwitch, type SwitchProps } from '@formwerk/core';

const props = defineProps<SwitchProps>();

const { inputProps, labelProps, isPressed, errorMessage, errorMessageProps } =
  useSwitch(props);
</script>

<template>
  <div>
    <label v-bind="labelProps">
      <input v-bind="inputProps" type="checkbox" class="sr-only" />

      <span>{{ isPressed ? '🌒' : '🌖' }}</span>
      {{ label }}
    </label>

    <div v-if="errorMessage" v-bind="errorMessageProps">{{ errorMessage }}</div>
  </div>
</template>

<style scoped>
label {
  display: inline-flex;
  align-items: center;
  cursor: pointer;
  border-radius: 99999px;
  background-color: #f1f1f1;
  padding: 0.5rem 1rem;
  gap: 0.25rem;
  font-size: 13px;
  transition: background-color 0.2s;
  user-select: none;

  &:has(:checked) {
    background-color: #2c3e50;
    color: #fff;
  }

  &:has(:focus) {
    box-shadow: 0 0 0 0.1rem rgba(0, 123, 255, 1);
  }
}

/** This is a common utility CSS class, you can find it in your CSS framework of choice */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  border: 0;
}
</style>
```
