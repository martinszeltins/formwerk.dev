---
import PhArrowRight from '~icons/ph/arrow-right';

const features = [
  {
    title: 'Aria Semantics',
    description:
      'Formwerk components adhere to the <a href="https://www.w3.org/WAI/ARIA/apg/" target="_blank" rel="noopener">W3C ARIA Authoring Practices Guide (APG)</a>, particularly the <a href="https://www.w3.org/WAI/ARIA/apg/patterns/" target="_blank" rel="noopener noreferrer">patterns</a> for various input types. This ensures the correct use of aria attributes and keyboard interactions for each field.',
  },
  {
    title: 'Adaptive Attributes',
    description:
      'You can use any HTML markup you prefer. Aria attributes will automatically adjust based on the elements you choose.',
    learnMore: '/docs/a11y/adaptive-attributes',
  },
  {
    title: 'Audited by Experts',
    description:
      'All components undergo rigorous audits by accessibility experts and users of assistive technologies. For a comprehensive list of contributors, visit the <a href="/credits">credits</a> page.',
  },
];
---

<section>
  <h2 class="heading-grad">
    Thoughtfully
    <span class="highlight">accessible</span>.
  </h2>

  <div class="mt-10 grid grid-cols-1 gap-4 md:grid-cols-3">
    {
      features.map((f) => (
        <div class="flex flex-col items-center rounded-lg border border-transparent bg-zinc-800/60 p-6 transition-colors duration-500 hover:border-amber-500/20 lg:items-start">
          <h3 class="heading-grad !text-2xl font-semibold">{f.title}</h3>
          <p
            class="mt-3 text-balance text-center text-sm text-zinc-400 lg:text-left"
            set:html={f.description}
          />

          {f.learnMore && (
            <a href={f.learnMore} class="mt-6 inline-block text-sm">
              Learn more
              <PhArrowRight />
            </a>
          )}
        </div>
      ))
    }
  </div>
</section>

<style>
  section {
    background: radial-gradient(
      closest-corner,
      rgba(245, 158, 11, 0.09) 0%,
      rgba(24, 24, 27, 0.1) 70%
    );

    a {
      @apply text-amber-500 hover:text-amber-600;

      svg {
        @apply inline-block h-4 w-4;
      }
    }

    .highlight {
      @apply bg-gradient-to-b from-amber-400 to-amber-600 bg-clip-text text-transparent;
    }
  }
</style>
